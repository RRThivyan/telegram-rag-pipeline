version: "3.9"

services:
  rag-bot:
    build: .
    container_name: telegram_rag_bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
